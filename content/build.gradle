buildscript {
    apply from: "$rootProject.projectDir/gradle/buildscript.gradle", to: buildscript
}

description = "Jose Gradle Kotlin Demo - Content"
defaultTasks = ['aemContentDeploy']

apply plugin: 'com.cognifide.aem'

aemCompose {
    includeSubprojects()
}